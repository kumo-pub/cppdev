"use strict";(globalThis.webpackChunkwebsite=globalThis.webpackChunkwebsite||[]).push([[860],{8453:(e,t,n)=>{n.d(t,{R:()=>r,x:()=>o});var i=n(6540);const s={},a=i.createContext(s);function r(e){const t=i.useContext(a);return i.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function o(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(s):e.components||s:r(e.components),i.createElement(a.Provider,{value:t},e.children)}},9246:(e,t,n)=>{n.r(t),n.d(t,{assets:()=>c,contentTitle:()=>o,default:()=>l,frontMatter:()=>r,metadata:()=>i,toc:()=>d});const i=JSON.parse('{"id":"foundamentals/map/index","title":"High-Performance Map Container Selection Guide","description":"I. Containers and Their Underlying Technical Characteristics","source":"@site/docs/foundamentals/map/index.md","sourceDirName":"foundamentals/map","slug":"/foundamentals/map/","permalink":"/cppdev/docs/foundamentals/map/","draft":false,"unlisted":false,"editUrl":"https://github.com/facebook/docusaurus/tree/main/packages/create-docusaurus/templates/shared/docs/foundamentals/map/index.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"Format & Output (String Formatting and Output)","permalink":"/cppdev/docs/foundamentals/format/"},"next":{"title":"Turbo Hash Containers","permalink":"/cppdev/docs/foundamentals/map/turbo_hash"}}');var s=n(4848),a=n(8453);const r={},o="High-Performance Map Container Selection Guide",c={},d=[{value:"I. Containers and Their Underlying Technical Characteristics",id:"i-containers-and-their-underlying-technical-characteristics",level:2},{value:"II. Scenario-Based Selection and Technical Basis",id:"ii-scenario-based-selection-and-technical-basis",level:2},{value:"III. Coding and Integration Technical Points",id:"iii-coding-and-integration-technical-points",level:2},{value:"IV. Quick Selection Reference Table",id:"iv-quick-selection-reference-table",level:2}];function h(e){const t={code:"code",h1:"h1",h2:"h2",header:"header",p:"p",strong:"strong",table:"table",tbody:"tbody",td:"td",th:"th",thead:"thead",tr:"tr",...(0,a.R)(),...e.components};return(0,s.jsxs)(s.Fragment,{children:[(0,s.jsx)(t.header,{children:(0,s.jsx)(t.h1,{id:"high-performance-map-container-selection-guide",children:"High-Performance Map Container Selection Guide"})}),"\n",(0,s.jsx)(t.h2,{id:"i-containers-and-their-underlying-technical-characteristics",children:"I. Containers and Their Underlying Technical Characteristics"}),"\n",(0,s.jsx)(t.p,{children:"The core value of ordered containers lies in supporting range queries and ordered traversal. The technical differences between the two mainstream containers stem from their underlying storage structures, which directly determine their performance boundaries and applicable scenarios."}),"\n",(0,s.jsxs)(t.p,{children:["turbo::btree_map/set is implemented based on a ",(0,s.jsx)(t.strong,{children:"variant of B+ tree"}),". Compared with the red-black tree of ",(0,s.jsx)(t.code,{children:"std::map"}),", its node structure has been specially optimized\u2014non-leaf nodes only store key-value indexes instead of complete data, while leaf nodes are linked through a doubly linked list, with all data concentrated in the leaf layer. This structure allows batch loading of consecutive leaf nodes during a single range query, significantly reducing the number of CPU memory accesses, and the cache hit rate is over 30% higher than that of red-black trees. Its expansion adopts a ",(0,s.jsx)(t.strong,{children:"batch progressive migration"})," strategy, which avoids business blocking caused by a large number of node rotations like red-black trees, and can maintain more stable write performance in scenarios with millions of Key insertions. In terms of C++17 compatibility, it natively supports ",(0,s.jsx)(t.code,{children:"std::string_view"})," as the Key, eliminating the need for intermediate copying via ",(0,s.jsx)(t.code,{children:"std::string"}),", and is compatible with structured binding syntax, so there's no need to manually call ",(0,s.jsx)(t.code,{children:"first"})," and ",(0,s.jsx)(t.code,{children:"second"})," when traversing key-value pairs. The core reason for the permanent stability of iterators is that insertion and deletion operations only adjust node pointers without moving the memory addresses of existing data. However, the index structure of non-leaf nodes brings about a 10% additional memory overhead, which accounts for a higher proportion in small dataset scenarios (fewer than 10,000 Keys), making it difficult to reflect its advantages."]}),"\n",(0,s.jsxs)(t.p,{children:["tsl::ordered_map/set adopts a ",(0,s.jsx)(t.strong,{children:"hybrid architecture of hash table + doubly linked list"}),'. The hash table provides O(1)-level fast lookup capabilities, while the doubly linked list maintains the insertion order of data to achieve the requirement of "unordered insertion and ordered traversal". Its hash buckets use ',(0,s.jsx)(t.strong,{children:"chaining"})," to resolve conflicts, but the linked list nodes are optimized with cache line padding\u2014each node's size is aligned to 64 bytes to avoid false sharing caused by multiple nodes being squeezed into the same cache line. This makes its lookup performance about 20% higher than traditional chaining implementations. However, this container has an obvious technical shortcoming: it needs to rebuild the mapping relationship between hash buckets and linked list nodes during expansion. This process will cause temporary performance jitter, which is more obvious in high-frequency insertion scenarios with tens of thousands of operations per second. Therefore, it is more suitable for mixed scenarios with moderate insertion frequency and requirements for traversal order."]}),"\n",(0,s.jsx)(t.p,{children:"Unordered hash containers are the mainstream choice in projects. The differences in conflict resolution mechanisms and memory layouts among containers with different algorithms are the core factors determining their performance and applicable scenarios."}),"\n",(0,s.jsxs)(t.p,{children:["turbo::flat_hash_map/set with flat algorithm adopts ",(0,s.jsx)(t.strong,{children:"open addressing + quadratic probing strategy"}),'. All key-value pairs are stored in a continuous memory array, eliminating the pointer overhead of linked list nodes in traditional chaining. It chooses quadratic probing instead of linear probing as the probing strategy, aiming to avoid the "clustering effect" of linear probing\u2014where conflicting elements cluster together, leading to a sharp increase in probing steps. The step size of quadratic probing is ',(0,s.jsx)(t.code,{children:"i\xb2"})," (where ",(0,s.jsx)(t.code,{children:"i"})," is the number of probes), which can distribute conflicting elements more evenly in the array. In terms of memory layout, it strictly follows ",(0,s.jsx)(t.strong,{children:"64-byte cache line alignment"}),", and the size of each data block is adapted to the CPU's cache reading granularity, making the cache hit rate of small objects close to 100%, far higher than that of containers with linked list structures. In terms of C++17 feature support, its ",(0,s.jsx)(t.code,{children:"emplace"})," operation implements ",(0,s.jsx)(t.strong,{children:"perfect forwarding"}),', which can directly construct objects in the memory array, avoiding the redundant step of "constructing temporary objects first and then copying" in the Abseil version. As a result, the insertion performance of small objects is improved by more than 15%. It should be noted that the invalidation of its iterators stems from memory reallocation during expansion\u2014the address of the new array is different from the original array, and the original memory address pointed to by the iterator becomes invalid. Therefore, it is necessary to call ',(0,s.jsx)(t.code,{children:"reserve"})," to preallocate sufficient space before batch insertion. In addition, its hash seed is stored in ",(0,s.jsx)(t.strong,{children:"Thread Local Storage (TLS)"}),", and the TLS spaces of different processes are isolated from each other, leading to inconsistent hash values across processes and making it impossible to be directly used in serialization scenarios."]}),"\n",(0,s.jsxs)(t.p,{children:["The core technology of F14 algorithm containers (folly::F14Map/Set, melon::F14Map/Set) is the ",(0,s.jsx)(t.strong,{children:"hybrid addressing mechanism"}),", whose core design goal is to balance cache friendliness at low load and conflict resolution capabilities at high load. When the load factor is lower than 0.5, it uses open addressing; when the load factor exceeds 0.5, it automatically switches to chaining. This switching threshold is verified through a large number of actual tests\u20140.5 is the critical point of the cache advantage of open addressing, and the conflict probability will increase significantly beyond this point. It supports a high load factor of up to 0.9, which benefits from the ",(0,s.jsx)(t.strong,{children:"segmented hash bucket design"}),": the entire hash table is divided into multiple independent segments, and each segment handles conflicts independently, avoiding performance degradation caused by global conflicts. In terms of serialization support, it has a built-in ",(0,s.jsx)(t.strong,{children:"fixed hash seed"}),', which is stored in the metadata area of the container and can be directly reused during cross-process deserialization, fundamentally solving the problem of hash stability. During project implementation, targeted fixes have been made for the high-concurrency deadlock problem of the Folly version\u2014in the original implementation, when multiple threads lock different segments at the same time, lock order inversion may occur (thread 1 locks segment A first and then segment B, while thread 2 locks segment B first and then segment A). After the fix, all threads are forced to lock in the order of "segment number from smallest to largest", completely eliminating deadlocks. The melon version has streamlined the metadata of Folly, removing non-core functions such as statistical monitoring, reducing the metadata memory usage by 20%, and is more suitable for scenarios with tight memory resources such as edge nodes.']}),"\n",(0,s.jsxs)(t.p,{children:["The core competitiveness of tsl::robin_map/set lies in the ",(0,s.jsx)(t.strong,{children:"Robin Hood hashing conflict resolution mechanism"}),', which is an optimization for open addressing. In traditional open addressing, conflicting elements are placed in the next free bucket, causing elements to move further away from their ideal hash buckets and leading to a sharp increase in the number of probing steps during lookup. The core logic of Robin Hood hashing is "fair competition"\u2014when inserting an element, calculate its "distance" from the ideal bucket. If this distance is greater than the distance of the existing element in the current bucket, swap their positions to keep the element with the greater distance in the current bucket. This swapping logic can make all elements as close to their ideal buckets as possible, significantly reducing the number of probing steps during lookup. In scenarios with long string Keys, due to the higher probability of hash conflicts, the effect of this optimization is more obvious\u2014the number of probing steps is reduced by 40% compared with traditional chaining, and the conflict resolution efficiency is improved by 30%. Its hash algorithm adopts ',(0,s.jsx)(t.strong,{children:"fixed FarmHash"})," without random seeds, natively supporting cross-process serialization. At the same time, it is natively compatible with ",(0,s.jsx)(t.code,{children:"std::string_view"}),", avoiding data copying without additional conversion, and has significant advantages in scenarios with a high proportion of string Keys."]}),"\n",(0,s.jsxs)(t.p,{children:["turbo::node_hash_map/set adopts a ",(0,s.jsx)(t.strong,{children:"structure of chaining + independent node storage"}),". Each node contains a key-value pair and two pointers (prev and next). Insertion and deletion operations only need to adjust pointer directions without changing the memory address of the node, which is the core technical reason for the permanent stability of its iterators. The advantage of this structure is that it supports dynamic insertion and deletion of data during traversal, without the need to collect Keys first and then process them in batches like the flat algorithm. However, it has obvious shortcomings\u2014scattered node storage will lead to a significant decrease in CPU cache hit rate. Because the memory addresses of nodes are discontinuous, after the CPU reads one node each time, the next node is likely not in the current cache line, requiring a new memory access. Actual test data shows that in small object scenarios, its cache miss rate is 13 percentage points higher than that of flat algorithm containers, and the performance is therefore about 30% lower, making it only suitable for scenarios with rigid requirements for iterator stability."]}),"\n",(0,s.jsxs)(t.p,{children:["Atomic lock-free containers (folly::AtomicHashMap/Set, melon::AtomicHashMap/Set) are the optimal solution for high-concurrency read scenarios. Their core technology is ",(0,s.jsx)(t.strong,{children:"fine-grained atomic operations + double-word CAS mechanism"}),". Their hash table is divided into multiple segments, and the head of each segment is stored through an atomic variable. Read operations do not require locking and directly obtain the head pointer through ",(0,s.jsx)(t.code,{children:"atomic_load"}),'. In high-concurrency read scenarios with 32 threads, the QPS is more than 50% higher than that of locked containers. To address the classic ABA problem of atomic operations, it adopts a double-word CAS scheme of "pointer + version number"\u2014each node\'s pointer is accompanied by a 32-bit version number, which increments by 1 each time the node is modified. When a thread performs CAS, it not only compares the pointer value but also the version number, completely avoiding the problem where "thread 1 reads A, thread 2 modifies A to B and then back to A, and thread 1 mistakenly judges that A has not been modified". On this basis, the melon version has made ',(0,s.jsx)(t.strong,{children:"compressed storage optimization"}),': using the free high-order space of 64-bit addresses (most systems only use 48-bit addresses), it packs a 16-bit version number and a 48-bit pointer into a 64-bit variable. Compared with Folly\'s "64-bit pointer + 64-bit version number" scheme, the metadata memory usage is reduced by 50%, and the single-thread performance loss is only 3%, making it more suitable for resource-constrained scenarios such as edge nodes. It should be clearly stated that such containers are only applicable to ',(0,s.jsx)(t.strong,{children:"read-heavy and write-light scenarios with a read-write ratio greater than 20:1"}),". In high-concurrency write scenarios, multiple threads performing CAS on the head of the same segment will lead to a large number of retries and a sharp performance drop."]}),"\n",(0,s.jsx)(t.h2,{id:"ii-scenario-based-selection-and-technical-basis",children:"II. Scenario-Based Selection and Technical Basis"}),"\n",(0,s.jsxs)(t.p,{children:["For small dataset scenarios (such as function parameter passing and temporary data storage), turbo::flat_hash_map is preferred. The core technical basis is that its continuous memory layout has high memory utilization and does not require frequent node memory allocation during construction. In scenarios with 10,000 Keys, the construction speed is 30% faster than that of ",(0,s.jsx)(t.code,{children:"std::unordered_map"}),". If no additional dependencies are desired, ",(0,s.jsx)(t.code,{children:"std::unordered_map"})," can be used directly, as the performance difference between the two is negligible for small datasets."]}),"\n",(0,s.jsxs)(t.p,{children:["For large dataset scenarios storing small objects, turbo::flat_hash_map is still the first choice. The technical reason is that its cache line alignment design can maximize CPU cache hit rate and reduce memory access overhead. During implementation, it is necessary to call ",(0,s.jsx)(t.code,{children:"reserve"})," to preallocate space in advance. The recommended preallocation size is 1.2 times the actual data volume, which is a verified optimal threshold\u2014it can avoid expansion while not wasting too much memory. If memory resources are tight and the data load factor is higher than 0.7, folly::F14Map can be selected because its hybrid addressing mechanism has more stable performance under high load."]}),"\n",(0,s.jsx)(t.p,{children:'For high-concurrency read and low-concurrency write scenarios (such as configuration caching and dictionary query), turbo::flat_hash_map is selected for single-machine environments based on the technical basis of its excellent lookup performance and low memory usage. In concurrent environments, if the Map data is completely independent, atomic lock-free containers are selected because their lock-free read feature can maximize concurrent performance. If the Map data is strongly bound to business data (such as counters and user status), the slot hash sharding scheme must be adopted. The technical reason is that the built-in locks of the container cannot be linked with business locks; forced use will lead to lock order inversion and deadlocks. Manual sharding can strictly control the order of "locking business data first and then shard locks", balancing performance and data consistency.'}),"\n",(0,s.jsx)(t.p,{children:"For low-concurrency read and high-concurrency write scenarios (such as log burying points and data collection), folly::F14Map is selected for single-machine environments. The core technical basis is that its hybrid addressing mechanism has stable insertion and deletion performance under high load\u2014with a load factor of 0.86, the average probing length is only 1.275, and the performance attenuation is less than 5%. If dynamic insertion and deletion of data during traversal are required, turbo::node_hash_map is selected because its iterator stability feature eliminates the need for additional Key processing. In concurrent environments, if the Map data is independent, pmap containers (if with built-in shard locks) are selected. The granularity of shard locks is finer than that of global locks, which can reduce lock contention. If the Map data is bound to business, slot hash sharding is selected, and the number of shards is recommended to be equal to the number of CPU cores to maximize multi-core parallel efficiency."}),"\n",(0,s.jsxs)(t.p,{children:["The selection for scenarios with special requirements must be closely linked to technical characteristics: when ",(0,s.jsx)(t.code,{children:"std::string_view"})," is needed as a non-copy Key, tsl::robin_map is preferred because it natively supports it and has high hash conflict resolution efficiency. If turbo::flat_hash_map is used, a custom hash function based on ",(0,s.jsx)(t.code,{children:"std::string_view"})," is required to avoid temporary copying. When stable iterators are needed for traversal and modification, turbo::node_hash_map is selected for single-machine environments, and pmap::flat_node_map is selected for concurrent independent Maps. For serialization or cross-process transmission, only F14, Robin, or atomic containers can be selected. The technical reason is that they support fixed hash seeds, ensuring cross-process hash consistency. For memory-sensitive embedded/edge node scenarios, turbo::flat_hash_map is preferred. If high load support is required, melon::F14Map is selected, benefiting from its streamlined metadata design."]}),"\n",(0,s.jsx)(t.h2,{id:"iii-coding-and-integration-technical-points",children:"III. Coding and Integration Technical Points"}),"\n",(0,s.jsx)(t.p,{children:"In terms of iterator usage, the iterators of flat, F14, and Robin series containers will be invalidated due to expansion, with the core reason being memory address reallocation. Therefore, direct insertion and deletion during traversal are prohibited, and the Keys to be operated must be collected first before batch processing. Node series iterators are stable, but locks must be added in concurrent scenarios; otherwise, data races will occur, leading to node pointer confusion."}),"\n",(0,s.jsxs)(t.p,{children:['In terms of concurrent coding, for scenarios where Map is bound to business data, the number of shards in slot hash sharding must be equal to the number of CPU cores, and the locks and data of each shard must be aligned to 64-byte cache lines to avoid false sharing. The lock order must strictly follow "business lock \u2192 shard lock", which is a necessary condition to avoid deadlocks. For scenarios where Map data is independent, atomic lock-free containers are selected for high-concurrency reads, and pmap containers with built-in shard locks are selected for high-concurrency writes. Encapsulating single-machine containers with ',(0,s.jsx)(t.code,{children:"std::mutex"})," is prohibited. Actual tests show that the concurrent QPS of this scheme is only 60% of that of mature containers."]}),"\n",(0,s.jsx)(t.p,{children:"In terms of serialization coding, containers supporting fixed hash seeds are preferred, and the seed must be explicitly passed as a template parameter in the code. If turbo/Abseil containers must be used, a custom stateless hash function is required, such as one based on CityHash with a fixed seed passed in, to avoid the impact of process-local random seeds."}),"\n",(0,s.jsxs)(t.p,{children:["In terms of integration, turbo containers must enable C++17 compilation options (such as GCC's ",(0,s.jsx)(t.code,{children:"-std=c++17"}),"), otherwise ",(0,s.jsx)(t.code,{children:"std::string_view"})," and structured binding cannot be supported. For Folly containers, the lock order inversion problem must be fixed first; for melon containers, it is necessary to confirm that the ABA problem has been fixed. For tsl::robin_map, the hash seed configuration must be verified to ensure stability in serialization scenarios. Self-developed pmap containers only support independent Map scenarios, and slot hash sharding must be used in binding scenarios."]}),"\n",(0,s.jsx)(t.h2,{id:"iv-quick-selection-reference-table",children:"IV. Quick Selection Reference Table"}),"\n",(0,s.jsxs)(t.table,{children:[(0,s.jsx)(t.thead,{children:(0,s.jsxs)(t.tr,{children:[(0,s.jsx)(t.th,{children:"Business Scenario"}),(0,s.jsx)(t.th,{children:"Recommended Container"}),(0,s.jsx)(t.th,{children:"Core Technical Basis"})]})}),(0,s.jsxs)(t.tbody,{children:[(0,s.jsxs)(t.tr,{children:[(0,s.jsx)(t.td,{children:"Single-machine small/large dataset, performance + memory sensitivity"}),(0,s.jsx)(t.td,{children:"turbo::flat_hash_map"}),(0,s.jsx)(t.td,{children:"Continuous memory + cache line alignment, high memory utilization and cache hit rate"})]}),(0,s.jsxs)(t.tr,{children:[(0,s.jsx)(t.td,{children:"Insert/delete data during traversal (single-machine)"}),(0,s.jsx)(t.td,{children:"turbo::node_hash_map"}),(0,s.jsx)(t.td,{children:"Chaining with independent nodes, permanent iterator stability"})]}),(0,s.jsxs)(t.tr,{children:[(0,s.jsx)(t.td,{children:"Insert/delete data during traversal (concurrent independent Map)"}),(0,s.jsx)(t.td,{children:"pmap::flat_node_map"}),(0,s.jsx)(t.td,{children:"Built-in shard locks + stable iterators, balancing concurrency and dynamic operations"})]}),(0,s.jsxs)(t.tr,{children:[(0,s.jsx)(t.td,{children:"Data serialization/cross-process transmission"}),(0,s.jsx)(t.td,{children:"folly::F14Map/tsl::robin_map"}),(0,s.jsx)(t.td,{children:"Fixed hash seed, cross-process hash consistency"})]}),(0,s.jsxs)(t.tr,{children:[(0,s.jsx)(t.td,{children:"High-concurrency read (independent Map)"}),(0,s.jsx)(t.td,{children:"melon::AtomicHashMap"}),(0,s.jsx)(t.td,{children:"Lock-free read + compressed storage, excellent high-concurrency performance and low memory usage"})]}),(0,s.jsxs)(t.tr,{children:[(0,s.jsx)(t.td,{children:"High-concurrency write (independent Map)"}),(0,s.jsx)(t.td,{children:"pmap::flat_hash_map (concurrent version)"}),(0,s.jsx)(t.td,{children:"Built-in shard locks, fine lock granularity and low contention"})]}),(0,s.jsxs)(t.tr,{children:[(0,s.jsx)(t.td,{children:"Map bound to external data (concurrent)"}),(0,s.jsx)(t.td,{children:"Slot hash sharding (turbo::flat_hash_map + shard locks)"}),(0,s.jsx)(t.td,{children:"Controllable lock order, avoiding deadlocks and data inconsistency"})]})]})]})]})}function l(e={}){const{wrapper:t}={...(0,a.R)(),...e.components};return t?(0,s.jsx)(t,{...e,children:(0,s.jsx)(h,{...e})}):h(e)}}}]);